{
  "name": "netflix-translator",
  "version": "0.1.0",
  "description": "Chrome extension for Netflix subtitle translation POC",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && vite build",
    "preview": "vite preview",
    "test": "vitest",
    "test:ui": "vitest --ui",
    "lint": "tsc --noEmit",
    "test:extension-syntax": "npm run build && node --check dist/content-scripts/netflix.js && node --check dist/service-worker.js && node --check dist/page-world-observer.js && node --check dist/popup.js && node --check dist/options.js && node --check dist/shared/subtitle-parser.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.268",
    "happy-dom": "^20.9.0",
    "typescript": "^5.4.5",
    "vite": "^5.2.11",
    "vitest": "^1.6.0"
  }
}
